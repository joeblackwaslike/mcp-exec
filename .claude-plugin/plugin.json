{
  "name": "mcp-exec",
  "version": "0.3.0",
  "description": "Sandboxed code execution MCP server — keep intermediate results out of the context window",
  "author": "Joe Black",
  "license": "MIT",
  "type": "plugin",
  "mcp": {
    "servers": {
      "mcp-exec": {
        "command": "npx",
        "args": ["@joeblackwaslike/mcp-exec"],
        "description": "Sandboxed code execution with MCP tool access"
      }
    }
  },
  "commands": [
    {
      "name": "install-skill",
      "description": "Append mcp-exec skill loader to CLAUDE.md",
      "run": "npx --package=@joeblackwaslike/mcp-exec mcp-exec-install-skill"
    }
  ]
}
