import{_ as e,o as a,c as n,ak as t}from"./chunks/framework.BwzOvEE0.js";const u=JSON.parse(`{"title":"Architecture","description":"How mcp-exec's sandbox execution model works — subprocess isolation, session lifetime, and MCP shim generation","frontmatter":{"description":"How mcp-exec's sandbox execution model works — subprocess isolation, session lifetime, and MCP shim generation"},"headers":[],"relativePath":"developer/architecture.md","filePath":"developer/architecture.md","lastUpdated":1778311718000}`),o={name:"developer/architecture.md"};function i(l,s,c,p,r,d){return a(),n("div",null,[...s[0]||(s[0]=[t(`<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><h2 id="sandbox-execution-model" tabindex="-1">Sandbox execution model <a class="header-anchor" href="#sandbox-execution-model" aria-label="Permalink to &quot;Sandbox execution model&quot;">​</a></h2><p>When Claude calls the <code>exec</code> MCP tool, mcp-exec spawns a sandboxed subprocess (or reuses a persistent session context) and runs the submitted code inside it. The sandbox is an OS-level process boundary enforced by <code>@anthropic-ai/sandbox-runtime</code> (<code>srt</code>) — the same sandbox Claude Code uses for its built-in bash tool.</p><p>This differs from native tool calls in one important way: <strong>downstream MCP tool invocations inside the sandbox happen within that subprocess</strong>, not through Claude Code&#39;s tool-use event system. From CC&#39;s perspective, only one tool was called: <code>exec</code>.</p><h2 id="startup-flow" tabindex="-1">Startup flow <a class="header-anchor" href="#startup-flow" aria-label="Permalink to &quot;Startup flow&quot;">​</a></h2><p>When the mcp-exec server starts, it executes the following sequence:</p><ol><li>Read <code>SandboxRuntimeConfig</code> from <code>.claude/settings.json</code> files (project-scope, then user-scope)</li><li>Connect all downstream MCP clients from <code>mcp.json</code></li><li>Build tool catalog (summaries only, not full schemas)</li><li>Register ESM loader hooks for generated shims</li><li>Create <code>SessionManager</code> for Node state</li><li>Listen for <code>exec</code> and <code>tools</code> tool calls</li></ol><h2 id="key-design-decisions" tabindex="-1">Key design decisions <a class="header-anchor" href="#key-design-decisions" aria-label="Permalink to &quot;Key design decisions&quot;">​</a></h2><p><strong>Sessions</strong> — Implicit by default; all <code>exec</code> calls within a conversation share one session context. Pass an explicit <code>session_id</code> only when you need parallel isolation (e.g. two concurrent workflows that must not share <code>globalThis</code> state). Note that <code>globalThis.*</code> persistence is Node-only — Bash and Python runtimes are stateless.</p><p><strong>Thenable chaining</strong> — <code>exec()</code> returns a real <code>Promise&lt;ExecResult&gt;</code>, not a custom thenable. Use standard <code>.then()</code> chaining. The agent uses <code>execResult.stdout</code> to thread raw output between runtimes explicitly; there is no auto-piping.</p><p><strong>Runtime param</strong> — accepts a string shorthand (<code>&quot;node&quot;</code>, <code>&quot;bash&quot;</code>, <code>&quot;python&quot;</code>) or a config object:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;node&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, timeout</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> number, env</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Record</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">string, string</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>Sandbox policy (network domains, filesystem access) is global and configured in <code>settings.json</code> only — it is not per-call configurable.</p><h2 id="project-structure" tabindex="-1">Project structure <a class="header-anchor" href="#project-structure" aria-label="Permalink to &quot;Project structure&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/</span></span>
<span class="line"><span>  server.ts              ← MCP server entry, registers tools/exec</span></span>
<span class="line"><span>  catalog/</span></span>
<span class="line"><span>    index.ts             ← lazy-loaded tool catalog, substring AND semantic search</span></span>
<span class="line"><span>  sandbox/</span></span>
<span class="line"><span>    index.ts             ← exec dispatcher → ExecResult interface</span></span>
<span class="line"><span>    config.ts            ← reads CC settings.json files, maps to SandboxRuntimeConfig</span></span>
<span class="line"><span>    session.ts           ← SessionManager for Node state</span></span>
<span class="line"><span>    runtimes/</span></span>
<span class="line"><span>      node.ts</span></span>
<span class="line"><span>      bash.ts</span></span>
<span class="line"><span>      python.ts</span></span>
<span class="line"><span>  loader/</span></span>
<span class="line"><span>    hooks.js             ← ESM loader hooks for MCP shim generation</span></span>
<span class="line"><span>skills/</span></span>
<span class="line"><span>  SKILL.md</span></span>
<span class="line"><span>  ts-sdk-reference.md</span></span>
<span class="line"><span>  py-sdk-reference.md</span></span>
<span class="line"><span>  examples/</span></span>
<span class="line"><span>docs/</span></span>
<span class="line"><span>  prds/</span></span></code></pre></div>`,15)])])}const g=e(o,[["render",i]]);export{u as __pageData,g as default};
